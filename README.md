# Baika

Desktop stock screener for MOEX shares using PySide6, pandas, and MOEX ISS API.

## Развертывание (Команды для отчета)
1. Установка зависимостей:
   ```bash
   poetry install
    ```

2. Форматирование и проверка кода:
    
    
    
    ```Bash
    poetry run ruff format .
    poetry run ruff check .
    ```
    
3. Запуск тестов:
    
    
    
    ```Bash
    poetry run pytest
    ```

## 📂 Структура проекта (Project Structure)

```text
moex-screener/
├── .github/                       # Конфигурации CI/CD (опционально)
├── data/                          # Слои работы с физическими данными
│   └── fixtures/                  # Изолированные JSON-пакеты для тестирования и офлайн-разработки
│       ├── securities_corrupted.json       # Граничный случай: поврежденный JSON-пакет
│       ├── securities_empty.json           # Граничный случай: пустой ответ API (выходной день)
│       ├── securities_invalid_types.json   # Граничный случай: аномальные типы данных
│       ├── securities_missing_columns.json # Граничный случай: отсутствие критических колонок
│       ├── securities_real.json            # Актуальный слепок живого ответа MOEX ISS
│       └── securities_valid.json           # Базовый позитивный сценарий (SBER, GAZP и null-значения)
├── logs/                          # Каталог автоматического ведения логов (создается динамически)
│   └── moex_screener.log          # Асинхронный ротируемый лог-файл приложения
├── src/                           # Исходный код приложения (Архитектура MVC/MVVM)
│   ├── __init__.py
│   ├── main.py                    # Главная точка входа, инициализация Loguru и запуск цикла Qt
│   ├── api/                       # Сетевой уровень (Network Layer)
│   │   ├── __init__.py
│   │   └── client.py              # HTTP-клиент (httpx), обработчик сетевых статусов и валидатор структур
│   ├── core/                      # Ядро системы и конфигурации
│   │   ├── __init__.py
│   │   ├── config.py              # Инициализация логгера, константы пресетов колонок (Базовый/Проф)
│   │   └── parser.py              # ВЗЯЛ JSON -> СДЕЛАЛ MERGE -> ОЧИСТИЛ -> ОТДАЛ DATAFRAME
│   ├── models/                    # Аналитический и модельный уровень (Model Layer)
│   │   ├── __init__.py
│   │   └── table_model.py         # [В разработке] Кастомная Qt-модель данных для сопряжения Pandas и QTableView
│   └── views/                     # Графический интерфейс (View Layer)
│       ├── __init__.py
│       ├── filter_panel.py        # [В разработке] Панель управления фильтрами и валидация ввода
│       └── main_window.py         # Главное окно графического интерфейса (PySide6)
├── tests/                         # Модульное и интеграционное тестирование (Тестовое окружение)
│   ├── __init__.py
│   └── test_api.py                # Юнит-тесты сетевого клиента, мокание аномалий сети и проверка фикстур
|   └── test_parser.py             # Тесты аналитики Pandas
├── .gitignore                     # Список исключений для системы контроля версий Git
├── poetry.lock                    # Фиксация точных версий зависимостей проекта
├── pyproject.toml                 # Главный манифест проекта (настройки Poetry, Ruff линтера и Pytest)
└── README.md                      # Документация проекта, руководство по запуску и отчетность
```

## 🔬 Модульное и интеграционное тестирование (`pytest`)

В проекте используется фреймворк `pytest` совместно с плагином `pytest-qt` для изолированного тестирования бизнес-логики (`pandas`) и графических моделей (`PySide6`).

Для запуска тестов в различных режимах используйте следующие команды:

#### 1. Базовый запуск

Запуск всех тестов в проекте в стандартном режиме:

```bash
poetry run pytest

```

#### 2. Подробный (Verbose) режим

Выводит подробную информацию: имя каждого тест-кейса, его модуль и статус выполнения (`PASSED`/`FAILED`). **Рекомендуется для отчетов по практике:**

```bash
poetry run pytest -v

```

#### 3. Запуск конкретного тестового файла

Если вы работаете над отдельным модулем и не хотите ждать прогона всей тестовой базы:

```bash
# Запуск только тестов фильтрации и аналитики Pandas
poetry run pytest tests/test_analytics.py -v

# Запуск только тестов кастомной Qt-модели таблицы
poetry run pytest tests/test_model.py -v

```

#### 4. Запуск конкретного теста (по имени функции)

Позволяет изолированно отлаживать один целевой тест-кейс:

```bash
poetry run pytest tests/test_analytics.py::test_strict_zero_change_vs_null -v

```

#### 5. Остановка при первой ошибке (`-x`)

Флаг `-x` (maxfail=1) мгновенно прерывает сессию тестирования, как только один из тестов падает. Удобно при TDD-разработке и рефакторинге:

```bash
poetry run pytest -v -x

```

#### 6. Вывод отладочной печати (`-s`)

По умолчанию `pytest` перехватывает и скрывает всё, что пишется в `stdout` (например, вызовы `print()` или логи). Этот флаг заставляет отображать консольный вывод внутри тестов:

```bash
poetry run pytest -v -s

```

---

Отличный вопрос! Добавление инструкций по линтингу и форматированию кода в `README.md` — это важная часть культуры разработки в ООО «ПРОКСИ». Это гарантирует, что вся команда (и проверяющие в вузе) будут писать код в едином стиле, а Git-репозиторий останется чистым.

**Ruff** — это современный, невероятно быстрый линтер и форматировщик кода на Python, который заменяет собой Flake8, Black, isort и другие инструменты.

Вот готовый блок, который ты можешь скопировать и вставить в свой `README.md`. Он написан кратко, структурировано и в деловом стиле.

---

### 📝 Вставка для добавления в `README.md`


## 🛠️ Качество кода и стандарты разработки (Ruff)

В проекте используется **Ruff** для автоматического форматирования кода и статического анализа (поиска ошибок, неиспользуемых импортов и нарушений стиля PEP 8). 

Перед каждым коммитом разработчик **обязан** проверить свой код.

### 🚀 Команды для терминала

1. **Автоматическое форматирование кода** (заменяет Black/isort, выравнивает отступы, сортирует импорты):
```bash
poetry run ruff format .

```

2. **Статический анализ и поиск ошибок** (линтер):
```bash
poetry run ruff check .

```


3. **Автоматическое исправление безопасных ошибок** (удаление неиспользуемых импортов, исправление простых нарушений):
```bash
poetry run ruff check . --fix

```



### ⚙️ Настройка в IDE (VS Code / PyCharm)

Чтобы код форматировался автоматически при каждом сохранении файла (`Ctrl + S`):

* **VS Code:** Установите официальное расширение `Ruff` от Astral. В `settings.json` добавьте:
```json
"[python]": {
    "editor.defaultFormatter": "astral-sh.ruff",
    "editor.formatOnSave": true
}
```


* **PyCharm:** Установите плагин `Ruff` через `Settings -> Plugins`. Включите чекбокс `Run ruff format on save` в настройках плагина.

